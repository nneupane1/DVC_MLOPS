from setuptools import setup, find_packages

setup(
    name="src",
    version="0.0.1",
    description="its a Wine Quality package with MLOPs",
    author="nneupane1",
    packages=find_packages(),
    license="MIT"
)